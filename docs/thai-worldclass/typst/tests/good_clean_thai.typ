#import "../thai-worldclass.typ": thai-worldclass
#show: thai-worldclass

ภาษาไทยควรเริ่มจากความหมายก่อนการจัดหน้า และปล่อยให้ตัวจัดบรรทัดของ Typst ทำงานจากแหล่งข้อความที่สะอาด โดยไม่ฝัง thin space ของ Google Docs ลงในต้นฉบับ
